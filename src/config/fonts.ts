import { Open_Sans as Montserrat, Inter as FontSans } from "next/font/google"

export const fontSans = FontSans({
  subsets: ["latin"],
  variable: "--font-sans",
})

export const montserrat = Montserrat({
  subsets: ["latin"],
  variable: "--font-sans",
})
